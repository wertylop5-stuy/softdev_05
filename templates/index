<!DOCTYPE html>

<html>
<head>
<title> {{title}} </title>
</head>

<body>
	<table>
		<tr>
			<th>Job Class</th>
			<th>Percentage</th>
		</tr>
		{% for item in occupations %}
		<tr>
			<td><a href="{{occupations[item][1]}}">{{item}}</a></td>
			<td>{{occupations[item][0]}}</td>
		</tr>
		{% endfor %}
	</table> 
	<h1>{{randomOccupation}}</h1>
</body>
</html>
